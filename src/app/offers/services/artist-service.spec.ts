import { ArtistService } from './artist-service';

describe('ArtistService', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ArtistService()).toBeTruthy();
  });
});
